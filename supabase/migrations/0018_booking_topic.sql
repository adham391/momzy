-- ═══════════════════════════════════════════════════════════════
-- 0018_booking_topic.sql — موضوع اللقاء في الحجز
-- اللقاءات الفردية (الاستشارة مع هبة، اللقاء في الناصرة) تسأل الأم
-- عند التسجيل عن موضوع اللقاء، كي تستعدّ له هبة.
-- الحقل اختياري — الخدمات التي لا تسأل عنه (askTopic في Sanity) تتركه فارغًا.
-- يُطبَّق بعد 0001–0017.
-- ═══════════════════════════════════════════════════════════════

alter table public.bookings
  add column if not exists topic text;

comment on column public.bookings.topic is
  'موضوع اللقاء كما كتبته الأم — للخدمات التي تسأل عنه (askTopic في Sanity)';
