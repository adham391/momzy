-- ═══════════════════════════════════════════════════════════════
-- 0025_booking_city.sql — بلدة الأم في الحجز
-- تُسأل عنها كل مسجِّلة في نموذج التسجيل (إلزامية)، وتظهر لهبة في
-- /admin/bookings وفي إيميل الحجز وفي جدول اليوم على واتساب.
-- الحجوزات السابقة تبقى بلا بلدة (null) — لا تُطلب بأثر رجعي.
-- يُطبَّق بعد 0001–0024، **وقبل نشر الكود** — التسجيل يكتب في العمود.
-- ═══════════════════════════════════════════════════════════════

alter table public.bookings
  add column if not exists city text;

comment on column public.bookings.city is
  'بلدة الأم كما كتبتها عند التسجيل — لتعرف هبة من أين تأتي المسجِّلات';
