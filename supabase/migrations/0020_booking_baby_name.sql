-- ═══════════════════════════════════════════════════════════════
-- 0020_booking_baby_name.sql — اسم الطفل الكامل في الحجز
-- يُسأل مع تاريخ ميلاد الطفل في الخدمات ذات الفئة العمرية، وهو
-- إلزامي للمولود فقط (الحامل تسجّل بالموعد المتوقّع بلا اسم).
-- يظهر لهبة في /admin/bookings وفي إيميل الحجز.
-- يُطبَّق بعد 0001–0019، **وقبل نشر الكود** — التسجيل يكتب في العمود.
-- ═══════════════════════════════════════════════════════════════

alter table public.bookings
  add column if not exists baby_name text;

comment on column public.bookings.baby_name is
  'اسم الطفل الكامل — للخدمات ذات الفئة العمرية، والفارغ لطفل لم يُولد بعد';
