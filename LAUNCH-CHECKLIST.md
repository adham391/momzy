# 🚀 قائمة ما قبل الإطلاق — Momzy

> ملف مرجعي لكل ما يجب إنجازه قبل إطلاق momzyworld.com.
> ✅ = جاهز في الكود · 🔴 = يمنع الإطلاق · 🟡 = مهم · 🟢 = تحسين
> آخر تحديث: 19 سبتمبر 2026 — **يوم الإطلاق**: الموقع يعمل على `https://momzyworld.com`

---

## ✅ ما هو جاهز بالفعل (لا تحتاجين فعل شيء)

الكود مبني ومُختبَر بالكامل — بما فيه **دفعة حقيقية ناجحة على الموقع المنشور** (19 سبتمبر 2026):

- المتجر وصفحات المنتجات · السلة · الـ checkout المرحلي السلس
- **الدفع بـ HYP مدمج** (iframe داخل الموقع، SAQ A) + التحقق + تعليم الطلب مدفوعًا
- الطلبات والحجوزات في Supabase · لوحة الأدمن الكاملة
- **إدارة المواعيد برزنامة** (إضافة، تكرار أسبوعي، تعديل، تحريك يوم، إلغاء مع إبلاغ المسجِّلات)
- **تذكير اليوم السابق** يحمل رابط الزوم أو المكان (Vercel Cron يومي)
- إيميلات التأكيد (Resend) · النشرة البريدية · خصم المخزون التلقائي
- **الشحن المجاني لكل منتج** من حقل «شحن مجاني» في Studio (الصندوق)
- **محركات البحث**: خريطة الموقع، بيانات منظّمة، Google Search Console وBing، وIndexNow
- إشعارات واتساب لهبة (مبنية، تنتظر مفاتيح Meta)
- تصدير CSV لشركة الشحن · التحليلات و UTM

**المتبقّي كله إعدادات حسابات خارجية — لا برمجة.**

---

## 🔴 1. الدفع — HYP (يمنع الإطلاق)

| # | الخطوة | المسؤول |
|---|--------|---------|
| 1.1 | تسجيل النشاط التجاري (עוסק) — شرط لفتح حساب HYP | هبة |
| 1.2 | فتح حساب **Hyp Pay** (وليس Hyp Enterprise) | هبة |
| 1.3 | الحصول على بيانات الترمينال الثلاثة: `Masof` · `KEY` · `PassP` | هبة |

- [x] **1.4 — إضافة المفاتيح** في `.env.local` **وفي Vercel** — مضبوطة، والدفع يعمل على الموقع المنشور:
```env
HYP_MASOF=رقم_الترمينال
HYP_KEY=مفتاح_API
HYP_PASSP=كلمة_مرور_API
```
> بمجرّد وجودها يتفعّل الدفع الإلكتروني **تلقائيًا**. بدونها يبقى التدفّق اليدوي (طلب → صفحة تأكيد).

- [x] **1.5 — ⚠️ عنوان العودة (الأهم)** في بوابة HYP — ضُبط في 19 سبتمبر 2026، وتحقّقنا: بعد الدفع عادت الصفحة إلى `momzyworld.com/order/…` والطلب مُعلَّم «مدفوعًا»:
> `Settings → Payment Page and API → Post-transaction address`
> ```
> https://momzyworld.com/api/hyp/callback
> ```
> **بدونه لن يُعلَّم الطلب «مدفوعًا» بعد الدفع إطلاقًا.**

- [ ] **1.6 — رفع شعار Momzy** (يستبدل «YOUR LOGO HERE»):
> `הגדרות → דף תשלום ו-API → פרטי העסק → +` وارفعي PNG/JPEG

- [ ] **1.7 — (اختياري) صفحة بطاقة فقط:** اتصلي بدعم HYP (‎*6488‎ تحويلة 3) واطلبي إخفاء حقول «بيانات العميل» و«العنوان» — لأننا نمرّرها مُعبّأة أصلًا.

- [x] **1.8 — اختبار حي:** 19 سبتمبر 2026 — طلب كتيب بـ ₪1 ببطاقة حقيقية على `momzyworld.com`: مُعلَّم «مدفوع»، ووصل إيميل التأكيد ورابط قراءة الكتيب وإشعار الطلب لصندوق Momzy. حُذفت طلبات التجربة بعدها.
  - [ ] **إرجاع مبالغ التجارب** من لوحة HYP (Refund): ₪67 · ₪250 · ₪1.

- [ ] **1.9 — Apple Pay:** جانب الموقع جاهز — ملف التوثيق منشور ومطابق لملف HYP على
  `https://momzyworld.com/.well-known/apple-developer-merchantid-domain-association`
  (الدومين الأساسي بلا `www` عمدًا — Apple لا تتبع التحويلات)، وإطار الدفع يسمح بالدفع (`allow="payment"`)،
  وسكربت HYP لـ Apple Pay داخل iframe محمَّل مع إطار الدفع (`EmbeddedPayment`). المتبقي في لوحة HYP:
  - [ ] **تفعيل Apple Pay:** הגדרות → Apple Pay → «אפשר תשלום באמצעות Apple Pay בדף התשלום» → שמור הגדרות (إن لم يظهر الخيار: اطلبي من HYP تفعيله على الترمينال).
  - [ ] **تسجيل الدومين:** في الصفحة نفسها «הוסף דומיין חדש» ← `momzyworld.com` (بلا `https://`).
  - [ ] **فحص:** iPhone بـ Safari ← صفحة الدفع ← زرّ Apple Pay ظاهر. (Google Pay وbit معطّلان أيضًا في الترمينال — من الإعدادات نفسها إن أردتِهما.)

---

## 🔴 2. Supabase (يمنع الإطلاق)

- [ ] **2.1 — ⚠️ الترقية لخطة مدفوعة.** الخطة المجانية **توقف المشروع تلقائيًا عند الخمول** — حدث فعلًا أثناء التطوير وعطّل كل الطلبات. في الإنتاج يعني ذلك **توقّف المتجر بالكامل**. وهي أيضًا بلا نسخ احتياطية يومية.
- [x] 2.2 — المشروع **نشط (Active)** — تحقّقنا في 19 سبتمبر 2026.
- [x] 2.3 — كل الـ migrations مُطبّقة (`0001` → `0019`).
- [x] 2.4 — حسابات الأدمن مُنشأة (`npm run seed:admins`). كلمة المرور تُغيَّر من `/admin/account`.
- [x] 2.5 — **حُذفت كل بيانات الاختبار** في 19 سبتمبر 2026: الطلبات والحجوزات والكوبونات والمواعيد ومشتركات النشرة وحسابات «مكتبتي» والتحليلات (مع نسخة احتياطية)، وأُعيد المخزون وحُرّرت المقاعد.

---

## 🔴 3. النشر على Vercel (يمنع الإطلاق)

- [x] **3.1 — متغيّرات البيئة في Vercel:** Supabase · Sanity · Resend · HYP · `CRON_SECRET` — تعمل على الموقع المنشور. (واتساب والتتبّع في بنديهما 5 و8.)
- [x] **3.2 — رابط الموقع** — مضبوط في Vercel (Production) وتحقّقنا منه في اللوحة:
```env
NEXT_PUBLIC_SITE_URL=https://momzyworld.com
```
- [x] 3.3 — ربط الدومين `momzyworld.com` بـ Vercel + شهادة SSL — 19 سبتمبر 2026.
  DNS يبقى في **Namecheap**: `A @ → 216.198.79.1` و`CNAME www →` عنوان Vercel، و`www` يحوّل (308) إلى `momzyworld.com`.
  > ⚠️ لا تنقلي الـ Nameservers إلى Vercel، ولا تلمسي سجلات البريد (MX · SPF · DKIM · DMARC · `send`) ولا سجلَّي `google-site-verification`.
- [x] 3.4 — نشر وتجربة الموقع على الدومين الحقيقي (صفحات، دفع، إيميلات، `/studio`).
- [x] **3.5 — `CRON_SECRET` في Vercel (Production)** — مضبوط، وتحقّقنا: `/api/cron/reminders` يقبل المفتاح ويرفض غيره. Vercel يشغّله يوميًا (`vercel.json`) — **بدونه لا يصل تذكير اليوم السابق**، وهو البريد الذي يحمل رابط الزوم أو المكان. والمفتاح نفسه يحمي بلاغ IndexNow اليومي (بند 12).

---

## 🟡 4. الإيميلات (Resend)

- [x] 4.1 — توثيق النطاق `momzyworld.com` في Resend — **Verified** (تحقّقنا عبر Resend API). الإرسال من `noreply@momzyworld.com` يصل لأي عنوان.
- [x] 4.1ب — بريد النطاق عبر **Google Workspace**: MX `smtp.google.com` · SPF · DKIM · DMARC واحد (`p=none`).
> تحقّقنا من خادم Namecheap ومن DNS العام معًا، وسجلات Resend (`send` · `resend._domainkey`) سليمة.
- [x] 4.1ج — DKIM مُفعَّل في Google، والمفتاح المنشور في DNS مطابق للمعروض في اللوحة.
- [x] 4.1د — المجموعات `orders@` · `workshops@` · `booklets@` · `hello@` (Who can post = External، والمحادثات غير مرئية للخارج).
  تحقّقنا بإرسال من مسار الموقع نفسه (Resend): وصلت الرسائل الأربع إلى البريد الوارد لهبة، وفلاتر Gmail في حسابها تصنّفها تحت `orders` · `workshops` · `booklets` · `hello` (ولا تُرسَل إلى البريد المزعج).
- [ ] **4.1هـ — مستخدم مدفوع واحد: `heba@`** (القرار: حذف `adham@` لتوفير مقعد). بالترتيب:
  1. ✅ من `adham@`: إعطاء `heba@` دور **Super Admin** — قبل أي حذف.
  2. ✅ الدخول بـ `heba@`: تفتح لها `admin.google.com` (ومنها حُذف `adham@`) · عضويتها في المجموعات الأربع (وصلتها رسائل الاختبار الأربع؛ مجموعة بلا أعضاء تقبل البريد ولا توصله لأحد).
  3. ✅ من `heba@`: حُذف `adham@` في 14 سبتمبر 2026 — حذفًا لا أرشفةً (الأرشفة تتطلّب رخصة مدفوعة). يُستعاد خلال 20 يومًا إن لزم.
  4. ✅ الاسم البديل `admin@` على `heba@` (Google سلّم رسالة اختبار ولم ترتدّ).
  5. ✅ لوحة الموقع: القرار إبقاء الحسابات الثلاثة كما هي (`adham@` · `heba@` · `admin@`). رابط «نسيت كلمة المرور» يصل لـ`heba@` و`admin@` (بريد هبة)؛ أمّا `adham@` فبلا صندوق، فعند الحاجة: `npm run admin:password -- adham@momzyworld.com`.
  6. ✅ الخطة: **Business Standard** (Flexible) — نُقلت من Business Plus، والقرار البقاء عليها. وصل بعد النقل بريد اختبار إلى `heba@` وإلى `hello@`.
  7. **إضافة وسيلة دفع قبل انتهاء التجربة (نحو 28 سبتمبر 2026)** — بدونها يُوقَف الحساب ويتوقّف البريد وإشعارات الموقع.
- [x] 4.2 — مفتاح `RESEND_API_KEY` مضبوط، والقوالب جاهزة ومُختبَرة.
- [x] **4.3 — وضع عناوين المجموعات في اللوحة** — ضُبطت في جدول `settings` وتحقّقنا منها بقراءتها من جديد (الموقع يقرؤها مع كل إشعار، بلا تخزين مؤقّت)
  في `/admin/settings` ← بطاقة
  **«وجهات الإشعارات»**:

| الصندوق | يستقبل |
|---------|--------|
| `orders@momzyworld.com` | طلبات فيها منتج فيزيائي — ما يُشحن |
| `workshops@momzyworld.com` | تسجيلات الورشات واللقاءات |
| `booklets@momzyworld.com` | مبيعات الكتيبات الرقمية |
| `hello@momzyworld.com` | رسائل نموذج «تواصلي معنا» — وهو العنوان الظاهر للعميلات في الإيميلات |

> الطلب الذي يجمع منتجًا وكتيبًا يصل الصندوقين معًا.
> **الحقل الفارغ يعود إلى `RESEND_TO_EMAIL`** — فلا يضيع إشعار قبل إنشاء الصناديق،
> والبند غير مُعطِّل للإطلاق.

- [x] **4.4 — النشرة البريدية في Resend**
  1. ✅ قائمة **«Momzy Newsletter»** (Segment) أُنشئت في Resend، والموقع يضيف إليها كل مشتركة من نموذج الفوتر تلقائيًا. اختبرناها حيّةً: الاشتراك يضعها في القائمة، والاشتراك من جديد بعد الإلغاء يعيد تفعيلها. ومن تؤشّر عند الدفع خانة الموافقة على الرسائل الدعائية تُضاف إليها أيضًا بعد نجاح دفعها.
  2. ✅ `RESEND_NEWSLETTER_SEGMENT_ID` مضبوط في Vercel (Production) — تحقّقنا باشتراك على الموقع المنشور وصل إلى القائمة. بدونه يُحفظ الاشتراك في Supabase ولا يصل إلى Resend.
  3. ✅ **صفحة إلغاء الاشتراك على الموقع نفسه** (`/newsletter/unsubscribe`، بالعربية والعبرية والإنجليزية) بدل صفحة Resend الإنجليزية:
     رابط تذييل كل نشرة يحمل بريد المستلِمة ورمزها (خاصية `unsubscribe_token` على جهة الاتصال — أُنشئت مرة في Resend، وتُعطى لكل مشتركة عند إضافتها)،
     وفتح الرابط يعرض التأكيد فقط، والإلغاء بزرّ — فيُسجَّل في Resend وSupabase معًا. اختُبرت: رمز خاطئ ← «الرابط غير صالح» · تأكيد ← ملغى في الاثنين · فتح الرابط بعدها ← «اشتراككِ ملغى».
  4. ✅ القالب والأمر جاهزان، وأُرسل العدد التجريبي `2026-09-welcome` إلى بريد تجربة ووصل Resend بحالة «sent». كل عدد جديد:
     ملفّ في `newsletters/` ← `npm run newsletter -- <العدد> --test <بريد>` للتجربة (قائمة «Momzy Newsletter — Test» وحدها)
     ← `npm run newsletter -- <العدد> --draft` مسودة للقائمة الحقيقية ← مراجعتها وإرسالها من لوحة Resend (**Broadcasts**).
     كلمة «פרסומת» في العنوان، والتذييل (Momzy · هبة حسن · بريد التواصل)، ورابط إلغاء الاشتراك — كلها في القالب نفسه فلا تُنسى.
  5. ✅ قائمة المشتركات نُظّفت من اشتراكات التجربة يوم الإطلاق — تبدأ من الصفر.
> الخطة المجانية لإيميلات التسويق في Resend تتّسع لـ1,000 جهة اتصال (تحقّقي من resend.com/pricing).
> إلغاء الاشتراك من صفحة الموقع يُسجَّل في Resend (فلا تصلها نشرة) وفي جدول `newsletter_subscribers` معًا.

---

## 🟡 5. إشعارات واتساب لهبة (Meta Cloud API)

- [ ] 5.1 — حساب **Meta Business** + رقم واتساب أعمال.
- [ ] 5.2 — الحصول على المفتاحين ووضعهما في `.env.local` + Vercel:
```env
WHATSAPP_PHONE_NUMBER_ID=معرّف_الرقم
WHATSAPP_ACCESS_TOKEN=التوكن_الدائم
```
- [ ] **5.3 — إنشاء قالب الرسالة** في Meta باسم `momzy_notification`
  (فئة **UTILITY** · لغة **ar** · **جسم بأربعة بارامترات** · بلا هيدر/أزرار) — انسخي النص:
```
🌸 {{1}} — Momzy
🔖 {{2}}
👤 {{3}}
📋 {{4}}
```
> الكود يملؤها: {{1}} النوع (طلب/حجز جديد) · {{2}} الرقم · {{3}} العميلة (اسم · هاتف) · {{4}} التفاصيل.
> **الاسم وعدد البارامترات يجب أن يطابقا القالب** وإلا يرفض Meta الإرسال.
- [x] 5.4 — رقم واتساب هبة مُدخَل في `/admin/settings`.

---

## 🟡 6. المحتوى (من Sanity Studio — `/studio`)

- [x] 6.1 — **صور المنتجات الحقيقية** مرفوعة (الصندوق والكتيب)، والموقع يطلبها بحجم الشاشة.
- [ ] 6.2 — إضافة باقي المنتجات (حاليًا: صندوق مشوار أم + كتيب وقت البطن).
- [x] 6.3 — **الكمية المتوفرة** (`stockQuantity`): الصندوق **1000** — ضُبط في 19 سبتمبر 2026. المخزون يُخصم تلقائيًا مع كل طلب ويعود عند الإلغاء؛ الكتيبات رقمية بلا مخزون.
- [x] 6.4 — إعداد `siteSettings`: رسالة الشريط العلوي بثلاث لغات (خانة لكل لغة في `/admin/settings`) · روابط السوشيال · بيانات التواصل.
- [ ] 6.5 — **رفع صور الغلاف للخدمات** (رُفعت نصوصها لـ Sanity، والصور لا تُرفع تلقائيًا).
- [ ] 6.6 — مراجعة **الفئة العمرية** لكل ورشة: حقلا «أصغر/أكبر عمر بالأشهر» يمنعان التسجيل خارج الفئة تلقائيًا. مضبوطة الآن: الأيام الأولى `0–3` · الحسية `4–12` · الحركة `8–14` · اللقاء الفردي `0–12` · ورشة الحوامل **بلا تحقّق عمدًا**. اتركي الحقلين فارغين لأي ورشة بلا شرط عمر.
- [x] 6.7 — **سعر الصندوق**: ₪490 بدل ₪549 (يظهر مشطوبًا مع «وفّري ₪59») · **شحن مجاني** (حقل «شحن مجاني» في Studio — يُسقط رسوم الشحن ويُظهر الشارة على البطاقة).

> ⚠️ **Sanity هو مصدر الخدمات الآن** — لا تُعيدي تشغيل `migrate-services-to-sanity.ts` بعد التعديل من Studio؛ يتخطّى الموجود افتراضيًا، و`--force` يُرجع نصوص الـ seed (لكنه يحفظ الصور).

---

## 🟡 7. إعدادات اللوحة (`/admin/settings`)

- [x] 7.1 — رقم واتساب هبة للإشعارات.
- [x] 7.2 — رسوم الشحن ₪35 للمنتجات بلا شحن مجاني · الصندوق بشحن مجاني من Studio.
- [x] 7.3 — المتجر والحجوزات **مفتوحان**.
- [x] 7.4 — **جلسات الورشات** من `/admin/bookings/availability` (رزنامة شهرية): أُضيفت الجلسات الأولى (23 سبتمبر 2026)، وحُذفت جلسات التجربة. رابط الزوم الثابت وعنوان الحضوري مضبوطان في الإعدادات ويُعبَّآن تلقائيًا في كل جلسة جديدة.
> التسجيل صار إلكترونيًا بالكامل: المقعد يُحجز فور التسجيل، والدفع عبر HYP، ورابط اللقاء (أو المكان) **لا يُرسل فور الدفع** بل في **بريد تذكير في صباح اليوم السابق** (ويظهر في صفحة تسجيلها حينها). عند اكتمال المقاعد تُفتح **قائمة انتظار** تلقائيًا — تابعيها من `/admin/bookings/waitlist`.

---

## 🟢 8. التتبّع والتحليلات

- [ ] 8.1 — إضافة المعرّفات في Vercel ثم Redeploy (السكربتات مبنية ومشروطة — صامتة بدونها؛ **غير مفعّلة حتى يوم الإطلاق**):
```env
NEXT_PUBLIC_META_PIXEL_ID=
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_GTM_ID=
```

---

## 🟢 9. قانوني

- [ ] 9.1 — **عند التسجيل كـ עוסק מורשה:** إضافة رقم ח.פ. واسم النشاط القانوني في الفوتر (**إلزامي قانونيًا**).
- [ ] 9.2 — مراجعة نصّي سياسة الخصوصية والشروط والأحكام.
- [ ] 9.3 — إضافة العنوان الفيزيائي للفوتر إن وُجد مكتب/عيادة.

---

## 🟢 10. متبقٍ في الكود (تطوير)

- [ ] 10.1 — ربط `ProductTestimonials` بجدول `reviews` في Supabase (حاليًا نصوص مؤقتة).
- [ ] 10.2 — إيميل تلقائي **للمستلِمة** عند طلب هدية.
- [x] 10.3 — الصور الحقيقية تُعرض عبر `ProductImagePlaceholder` (يبقى احتياطًا حين لا توجد صورة).

---

## ✅ 11. اختبار نهائي على الإنتاج (بعد كل ما سبق)

- [x] طلب كامل ببطاقة حقيقية → صفحة التأكيد تعرض «مدفوع» (19 سبتمبر 2026)
- [x] وصول إيميل التأكيد للعميلة + إيميل الإشعار لهبة
- [ ] وصول إشعار واتساب لهبة
- [x] ظهور الطلب في `/admin/orders` + خصم المخزون في Sanity (وإرجاعه عند حذف التجربة)
- [x] حجز خدمة من `/services/[slug]` → يظهر في `/admin/bookings`
- [ ] تصدير CSV لعناوين الشحن من لوحة الطلبات
- [ ] نموذج التواصل + الاشتراك في النشرة
- [ ] تجربة كاملة على **الجوال** والديسكتوب
- [x] حذف كل البيانات التجريبية (19 سبتمبر 2026)

---

## 🟢 12. محركات البحث (SEO)

- [x] 12.1 — **خريطة الموقع** `https://momzyworld.com/sitemap.xml` (63 رابطًا بثلاث لغات، تتجدّد كل ساعة) و`/robots.txt` (يمنع أرشفة اللوحة وصفحات العميلات الخاصة).
- [x] 12.2 — **بيانات منظّمة** (العلامة وهبة، المنتج بسعره وشحنه المجاني، المقال، الخدمة، مسار التنقّل) · روابط أساسية (canonical) · ربط اللغات (hreflang) · صورة معاينة للمشاركة. عنوان الرئيسية فيه «مومزي / מומזי».
- [x] 12.3 — **Google Search Console**: ملكية النطاق موثّقة بسجل TXT في Namecheap (**لا تحذفيه** — Google يعيد فحصه)، الخريطة مُرسلة، وطُلبت أرشفة الصفحات الرئيسية — 19 سبتمبر 2026.
- [x] 12.4 — **Bing Webmaster Tools**: الخريطة ناجحة (63 رابطًا)، والروابط الرئيسية مُرسلة. Bing يغذّي أيضًا DuckDuckGo وChatGPT وCopilot.
- [x] 12.5 — **IndexNow** مفعّل: المفتاح على `/indexnow-key.txt`، وأُرسل بلاغ بكل الصفحات (قُبل)، ومهمة يومية تبلّغ Bing بكل ما يتغيّر في Sanity.
- [ ] 12.6 — (اختياري، للتبليغ الفوري) **webhook في Sanity** ← `https://momzyworld.com/api/revalidate?secret=<SANITY_REVALIDATE_SECRET>` (POST · Create/Update · Projection `{_type, slug}`) + نفس القيمة في Vercel. بدونه يكفي البلاغ اليومي.
- [ ] 12.7 — **ملف النشاط التجاري على Google** (Google Business Profile) لـ «Momzy — هبة حسن»: الموقع، إنستغرام، الصور، الخدمات — وتقييمات عميلات حقيقيات.
- [ ] 12.8 — رابط `momzyworld.com` في bio إنستغرام وتيك توك وقناة واتساب.

---

## ⚠️ مزالق مهمة (مستخلصة من الاختبارات الفعلية)

| المزلق | التفصيل |
|--------|---------|
| **Supabase يتوقف** | الخطة المجانية توقف المشروع عند الخمول → المضيف لا يُترجَم (`ENOTFOUND`) → **كل الطلبات تفشل**. الترقية ضرورية. |
| **عنوان العودة في HYP** | بدونه لن يعود HYP للموقع بعد الدفع، ولن يُعلَّم الطلب مدفوعًا أبدًا. |
| **نطاق Resend غير موثّق** | الإيميلات لن تصل لعميلات حقيقيات. |
| **قالب واتساب** | يجب أن يطابق الاسم وعدد البارامترات تمامًا وإلا يرفض Meta. |
| **الأسماء العربية على صفحة HYP** | **أُصلح** — صفحة HYP تُخدَم `windows-1255` (ترميز عبري لا يحوي الحرف العربي)، فكل نصّ عربي كان يظهر `???`. الكود ينقحر الاسم والعنوان إلى العبرية قبل الإرسال (`lib/hyp/text.ts`): «هبة حسن» ← «הבה חסן». لا إجراء مطلوب. |
| **قالب دفع مخصّص (תבנית לקוח)** | الترمينال مضبوط عليه والقالب غير معرَّف عند HYP → **كل طلب دفع يفشل** بـ`תבנית לא קיימת`. سلّمي HYP القوالب الثلاثة من `reference/hyp-template-{ar,he,en}.html` واشترطي تقديمها **UTF-8** (وإلا عاد النص العربي `???`). |
| **`Fild1` في رد HYP** | HYP يستبدله باسم العميلة — **الكود يعالجها** (يقرأ رقم الطلب من `Order`). لا إجراء مطلوب. |
| **نشر جاهز لا يصل للموقع** | حدث يوم الإطلاق: نشر بحالة **Ready** بقي خارج الموقع (بلا شارة Production الزرقاء). الحل: Vercel ← **Deployments** ← ⋯ ← **Promote**. |
| **متغيّر بيئة جديد في Vercel** | لا يصل للموقع إلا مع **نشر جديد** (Redeploy) — خصوصًا المتغيّرات التي تبدأ بـ `NEXT_PUBLIC_`. |
| **سجلات DNS في Namecheap** | الحفظ يفشل أحيانًا («Failed to save record») — حدّثي الصفحة واستعملي **SAVE ALL CHANGES**؛ وحفظ سجل موجود أصلًا يُرفض كتكرار. |

---

## 📌 الحد الأدنى للإطلاق

الخمسة الإلزامية — **حالتها يوم الإطلاق (19 سبتمبر 2026)**:

1. ✅ مفاتيح HYP الثلاثة + عنوان العودة `/api/hyp/callback`
2. ⬜ ترقية Supabase (كي لا يتوقف) — **الوحيد المتبقّي**
3. ✅ كل متغيّرات البيئة في Vercel + `NEXT_PUBLIC_SITE_URL` الحقيقي
4. ✅ توثيق نطاق Resend
5. ✅ صور المنتجات ومحتوى Studio

الباقي (واتساب · تتبّع · شهادات · إيميل الهدية) يمكن إضافته بعد الإطلاق بلا تعطيل.
